#pragma once
#include "AVL.h"
#include <math.h>
int compareInt(void* num1, void* num2);
int createArray(int size, AVL_TREE* arrList);
void printList(AVL_TREE* arrList);
void printNumber(void* aNumber);
int createList(int* arrt, AVL_TREE* arrList);

